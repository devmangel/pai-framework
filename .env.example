# AWS Configuration
AWS_REGION=us-west-2
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key

# DynamoDB Configuration
DYNAMODB_TABLE_PREFIX=dev_
DYNAMODB_ENDPOINT=http://localhost:8000

# Application Configuration
PORT=3000
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# LLM Configuration
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key

# Cache Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# WebSocket Configuration
WS_PORT=3001

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# Logging
LOG_LEVEL=debug

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual configuration
# 3. Never commit the actual .env file to version control
# 4. Keep this example file updated with any new environment variables

# Notes:
# - For local development, you can use DynamoDB Local
# - For production, remove DYNAMODB_ENDPOINT to use AWS DynamoDB
# - ALLOWED_ORIGINS can be a comma-separated list of domains
# - LOG_LEVEL options: error, warn, info, debug
# - NODE_ENV options: development, production, test
